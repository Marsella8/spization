# TODO: If a sync node has 1 either in or out you can simplify. NOT DUMMZY NODES< SYNC NODES


# TODO add
# def simplify_dummy_nodes(sp: SerialParallelDecomposition) -> SerialParallelDecomposition:
#     for node in [n for n in get_nodes(sp) if isinstance(n, DummyNode)]:
#         ...

# def simplify_dummy_nodes(g: DiGraph) -> SerialParallelDecomposition:
#     for node in [n for n in g.nodes() if isinstance(n, DummyNode)]:
#         ...
